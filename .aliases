alias ..="cd .."
alias cd..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."
alias cdp="cd ~/Projects"

alias mv="mv -v"
alias rm="rm -i -v"
alias cp="cp -v"

alias where=which

alias la="ls -al"
alias sl=ls

alias mkdir="mkdir -p"

alias vi=vim

alias gs="git status"
